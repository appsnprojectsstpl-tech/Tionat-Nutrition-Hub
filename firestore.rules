rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEBUG MODE: Allow everything (User Requested)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
